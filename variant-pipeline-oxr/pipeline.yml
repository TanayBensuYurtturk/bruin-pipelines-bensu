name: "{{ var.client }}_pipe"
schedule: "{{ var.schedule }}"

variables:
  client:
    type: string
    default: default_client
  region:
    type: string
    default: us
  schedule:
    type: string
    default: "@daily"

variants:
  client_alpha:
    client: alpha_oxr
    region: us
    schedule: "@hourly"
  client_beta:
    client: beta_oxr
    region: eu
    schedule: "0 6 * * *"
  client_gamma:
    client: gamma_oxr
    region: ap
    schedule: "@weekly"