name: Repo Demo Dashboard
description: Example repo-based (DAC) dashboard for testing the view toggles
connection: gcp-default

rows:
  - widgets:
      - name: Answer
        type: metric
        col: 12
        sql: |
          SELECT 42 AS value
