name: Repo Demo Dashboard

rows:
  - widgets:
      - name: Welcome
        type: text
        col: 12
        content: |
          # Repo Demo Dashboard

          A mock repo-based dashboard for testing. It uses a static **text**
          widget, so it needs no connection or query to render.

          Use the card's ⋮ menu to try **Disable chat** and **Hide YAML**.
