name: hooks_invalid_type
schedule: "@daily"

# `python` is not a SQL asset type, so `bruin validate` reports a lint error
# from the `default-hooks-applicable-types` rule.
default:
  hooks:
    applicable_type:
      - duckdb.sql
      - python
    pre:
      - query: "SELECT 1"
